# Why this is the ultimate setup:

    Zero Hallucinations: The agent isn't "guessing" how to implement your !['Find', '...'].contains(...) safe subscreen routing. The script explicitly hardcodes your genius solution into the parent file.

    Instant Implementation: When you say "Build a new Equipment tracker UI," the agent triggers this script, instantly dropping Equipment.xml, FindEquipment.xml, and EquipmentDetail.xml on your disk.

    Hyper-Focus: After the script runs, the agent spends its tokens solely on modifying the <field-layout> and <actions> blocks to add the specific database fields, referencing your moqui-screen-forms.md rules as it types.